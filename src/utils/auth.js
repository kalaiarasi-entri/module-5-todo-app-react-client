import jwtDecode from 'jwt-decode'; export const getToken=()=>localStorage.getItem('token'); export const isLogged=()=>!!getToken(); export const logout=()=>{ localStorage.removeItem('token'); window.dispatchEvent(new Event('authChange')); window.location.href='/login'; }; export const getUserFromToken=()=>{ const t=getToken(); if(!t) return null; try{return jwtDecode(t)}catch{return null} };
